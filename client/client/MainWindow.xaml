<Window x:Class="RockPapperScissorsWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RockPapperScissors Game" Height="480" Width="550" Background="GOLD">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <Label Content="COM Port:"/>
            <ComboBox x:Name="PortComboBox" SelectionChanged="PortComboBox_SelectionChanged" 
                      Margin="0,0,0,10"/>
            <Label Content="Baud Rate:"/>
            <ComboBox x:Name="BaudRateComboBox" 
          SelectionChanged="BaudRateComboBox_SelectionChanged"
          Margin="0,0,0,10">
                <ComboBoxItem Content="4800"/>
                <ComboBoxItem Content="9600"/>
                <ComboBoxItem Content="19200"/>
                <ComboBoxItem Content="38400"/>
                <ComboBoxItem Content="57600"/>
            </ComboBox>
            <Label Content="Game Mode:"/>
            <ComboBox x:Name="GameModeComboBox" SelectionChanged="GameModeComboBox_SelectionChanged">
                <ComboBoxItem Content="Player vs Player"/>
                <ComboBoxItem Content="Player vs Computer"/>
                <ComboBoxItem Content="Computer vs Computer"/>
            </ComboBox>
        </StackPanel>

        <Button Grid.Row="1" Content="Start Game" Click="StartGame_Click" Margin="10" Width="200"/>

        <TextBlock Grid.Row="2" x:Name="StatusText" Margin="10" TextAlignment="Center"/>

        <Grid Grid.Row="3" x:Name="GameGrid" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>

        <TextBlock Grid.Row="4" x:Name="ResultText" Margin="10" TextAlignment="Center"/>
    </Grid>
</Window>